[package]
name = "espidf-std"
version = "0.1.0"
authors = ["Erick Weil <erick.weil@ifro.edu.br>"]
edition = "2024"
resolver = "2"

[profile.release]
opt-level = "s"

[profile.dev]
debug = true    # Symbols are nice, and they don't increase the size on Flash
opt-level = "z"

[features]
# Feature base: habilita toda a stack ESP-IDF
espidf = ["dep:esp-idf-svc", "dep:embassy-time"]

# --- Chips suportados ---
# Adicionar aqui novos chips conforme necessário.
# Cada chip deve depender de "espidf" (e futuramente de features específicas do chip).

# Esp32-C3 SuperMini (RISC-V, target: riscv32imc-esp-espidf)
esp32c3 = ["espidf"]

# Heltec Wireless Tracker (ESP32-S3FN8, Xtensa, target: xtensa-esp32s3-espidf)
# esp32s3 = ["espidf"]

# O chip padrão para desenvolvimento local. Trocar conforme a placa em uso.
default = ["esp32c3"]

[dependencies]
log = "0.4"
esp-idf-svc = { version = "0.52.1", features = ["critical-section", "embassy-time-driver", "embassy-sync"], optional = true }
# Remove `generic-queue-8` if you plan to use `embassy-time` WITH `embassy-executor`
embassy-time = { version = "0.5", features = ["generic-queue-8"], optional = true }

[build-dependencies]
embuild = "0.33"
